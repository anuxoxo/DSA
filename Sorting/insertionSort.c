#include <stdio.h>

void insertionSort(int *arr, int n)
{
    for (int i = 0; i < n; i++)
    {
        for (int j = i; j < n - 1; j++)
        {
                }
    }
}

int main()
{
    int arr = {7, 12, 3, 4, 1}, n = 5;
    insertionSort(arr, n);
    return 0;
}