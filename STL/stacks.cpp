#include <iostream>
#include <stack>
using namespace std;

int main()
{
    stack<int> s;
    int n, e;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> e;
        s.push(e);
    }

    for (int i = 0; i < ; i++)
    {
        cout << s.top() << endl;
    }

    return 0;
}